# Production trên Vercel: thêm biến này trong Vercel → Environment Variables (không dấu / cuối).
# Ví dụ: VITE_API_BASE_URL=https://your-api.onrender.com
# Local: để trống — Vite proxy /api và /uploads tới localhost:5000.
# VITE_API_BASE_URL=
